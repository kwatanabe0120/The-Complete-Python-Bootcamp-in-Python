# Question: Create a program that adds two numbers and prints the result.

num1 = 3
num2 = 5
sum_result = num1 + num2
print("The sum is", sum_result)

# Explanation:
# We store two numbers in variables num1 and num2.
# Using the + operator, we add them and store the result in sum_result.
# Finally, print displays the message along with the calculated sum.
